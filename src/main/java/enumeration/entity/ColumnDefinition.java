package enumeration.entity;

/**
 * <p> map of data base column definitions
 *
 * @author alireza_bayat
 */
public enum ColumnDefinition {
    INT, BIGINT, TINYINT, VARCHAR, NVARCHAR, BLOB, CLOB, BOOL, DECIMAL, BIG_DECIMAL, DATE, DATETIME
}
